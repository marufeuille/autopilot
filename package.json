{
  "name": "claude-workflow-kit",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev:temporal": "npm run dev --workspace=apps/temporal",
    "dev:autopilot": "npm run dev --workspace=apps/autopilot"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  }
}

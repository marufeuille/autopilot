version: '3.8'

services:
  jaeger:
    image: jaegertracing/all-in-one:1.62
    ports:
      # OTLP gRPC receiver
      - '4317:4317'
      # OTLP HTTP receiver
      - '4318:4318'
      # Jaeger UI
      - '16686:16686'
    environment:
      - COLLECTOR_OTLP_ENABLED=true
